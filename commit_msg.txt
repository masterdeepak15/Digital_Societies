fix(mobile): add theme barrel index + fix OTP input visibility on Android

- Add apps/mobile/src/theme/index.ts barrel re-export
  WalletScreen.tsx imported from '../../theme' (index) but only individual
  theme files (colors/typography/spacing) existed — Metro bundler failed with
  "Unable to resolve module ../../theme"
- Fix LoginScreen OTP TextInput: add explicit color + backgroundColor tokens
  Android can render text as transparent without an explicit color; input now
  sets color: Colors.textPrimary and backgroundColor: Colors.surface so both
  the phone field and OTP field are clearly legible on all Android devices
